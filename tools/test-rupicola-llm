#!/bin/sh

set -eu

tool_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
export PYTHONDONTWRITEBYTECODE=1
export PYTHONPATH="$tool_dir${PYTHONPATH:+:$PYTHONPATH}"

exec python3 -m unittest discover -s "$tool_dir/tests" "$@"

